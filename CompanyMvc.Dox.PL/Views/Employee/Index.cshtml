

@model IEnumerable<Employee>
@{
    ViewData["Title"] = "Index";
}
<div class="w-100 m-5 text-center bg-dark shadow p-5 rounded-3"
    > <h1 class="text-white ">All Employee </h1></div>

<a asp-controller="Employee" asp-action="Create" class="btn btn-primary">Create Employee</a>
<br />
<br />

@if (Model.Count() > 0)
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(D => D.Name)</th>
                <th>@Html.DisplayNameFor(D => D.Age)</th>
                <th>@Html.DisplayNameFor(D => D.Email)</th>
                <th>@Html.DisplayNameFor(D => D.phoneNumber)</th>
                <th>@Html.DisplayNameFor(D => D.Salary)</th>
                <th>@Html.DisplayNameFor(D => D.Address)</th>
                <th>@Html.DisplayNameFor(D => D.HiringDate)</th>
                <th>@Html.DisplayNameFor(D => D.IsActivated)</th>
                <th>Details</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var emp in Model)
            {
                <tr>
                  
                    <td>@emp.Name</td
                    <td>@emp.Age</td>
                    <td>@Html.DisplayFor(D => emp.Email)</td>
                    <td>@emp.phoneNumber</td>
                    <td>@Html.DisplayFor(D => emp.Salary)</td>
                    <td>@emp.Address</td>
                    <td>@emp.HiringDate</td>
                    <td>@Html.DisplayFor(D => emp.IsActivated)</td>
                  

                    <td>
                        <a asp-controller="Employee" asp-action="Details" asp-route-id="@emp.Id" class="btn btn-warning">
                            <i class="fas fa-eye"></i> Details
                        </a>
                    </td>
                    <td>
                        <a asp-action="Update" asp-route-id="@emp.Id" class="btn btn-success">
                            <i class="fas fa-edit"></i> Update
                        </a>
                    </td>
                    <td>
                        <a asp-action="Delete" asp-route-id="@emp.Id" class="btn btn-danger">
                            <i class="fas fa-trash"></i> Delete
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

}
else
{
    <div class="mt-4 alert alert-warning">
        <h4>There is No Employee :(</h4>
    </div>
}



